# Claude Guide

Read `AGENTS.md` first. It is the canonical agent guide for this repository.

Claude-specific notes:

- Keep edits focused and prefer the repo's existing React, TypeScript, Storybook, and docs patterns.
- When changing component behavior, check implementation, types, tests, stories, and docs together.
- Do not edit generated outputs such as `docs/export` or `packages/open-ui-kit/dist`.
- Use Conventional Commit style for PR titles and commit messages.
