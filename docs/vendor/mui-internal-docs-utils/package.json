{
  "name": "@mui/internal-docs-utils",
  "version": "3.0.4",
  "author": "Open UI Kit maintainers",
  "description": "Utilities for Open UI Kit docs. This is an internal package not meant for general use.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/outshift-open/open-ui-kit.git",
    "directory": "docs/vendor/mui-internal-docs-utils"
  },
  "scripts": {
    "prebuild": "rimraf ./build",
    "build": "code-infra build --flat --skip-babel-runtime-check",
    "typescript": "tsc -p tsconfig.json",
    "release:publish": "pnpm build && pnpm publish --tag latest",
    "release:publish:dry-run": "pnpm build && pnpm publish --tag latest --registry=\"http://localhost:4873/\""
  },
  "dependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^5.9.3"
  },
  "publishConfig": {
    "access": "public",
    "directory": "build"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
