import { Meta } from "@storybook/addon-docs/blocks";
import * as LegacyIcons from "@/custom-icons";
import { DocsHeader } from "storybook/components/docs-header.stories";
import { IconGalleryDocs } from "./icon-gallery";

<Meta title="Foundations/Iconography (Legacy)" />

<DocsHeader
  title="Iconography (Legacy)"
  blurb={`Pre-Figma-migration icon set: ${Object.keys(LegacyIcons).length} icons across system UI and cloud providers. Superseded icons are marked @deprecated in source but remain available via LegacyIcons.*. Click any icon to copy its export name.`}
  importLine='import { LegacyIcons } from "@open-ui-kit/core";'
  includeStories={false}
/>

<IconGalleryDocs allIcons={LegacyIcons} />
